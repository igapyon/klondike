# Architecture

## 概要
- 単一 HTML（1ファイル）で動作する静的 Web アプリ。
- ルールは `GameLogic.md` を一次ソースとする。
- カード描画は CardMeister を利用する。

## ファイル構成
- `index.html`（予定）: HTML/CSS/JS をすべて内包。
- `GameLogic.md`: ゲームロジック仕様。
- `README.md`: 目的・方針・決定事項。
- `AGENTS.md`: 開発合意事項。

## 実行モデル
- 初期化: デッキ生成→シャッフル→場札配布→山札構築。
- 描画: 状態オブジェクトを元に DOM を再構築（差分最適化は後回し）。
- 操作: クリック/ドラッグで移動を発火（詳細は後決め）。
- 判定: 移動の正当性・勝利・詰みをロジックで判定。

## 状態管理
- 単一の `state` オブジェクトで管理。
- 変更は操作ハンドラ経由でのみ行う。

## UI 層
- カード: CardMeister の `<playing-card>` を使用。
- レイアウト: CSS で Tableau / Foundations / Stock / Waste を配置。
- 入力: クリック/ドラッグのどちらか（未確定）。

## 未確定事項
- 操作方式（クリック移動 / ドラッグ＆ドロップ）
- ルール詳細（リサイクル条件、詰み判定）
- セーブ/ロード（後回し）

